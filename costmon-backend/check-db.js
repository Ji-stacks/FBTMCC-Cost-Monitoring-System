const sqlite3 = require('sqlite3').verbose(); const db = new sqlite3.Database('fbtmcc.db'); db.all('SELECT * FROM projects', (err, rows) => { console.log(JSON.stringify(rows, null, 2)); db.close(); });
